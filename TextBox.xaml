<TextBox Width="200" 
         Height="30" 
         Text="Введите имя" 
         FontSize="14"
         Margin="10"
         PlaceholderText="Имя пользователя"
         IsReadOnly="False"
         AcceptsReturn="False"/>
